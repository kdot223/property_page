# 🏡 Property Page (All-in-One)
## วิธีใช้งาน
1. แตกไฟล์ zip
2. เปิด PowerShell หรือ Terminal แล้วพิมพ์:
   ```
   npm install
   npm run dev
   ```
3. ระบบจะรันทั้ง Next.js (port 3000) และ Express backend (port 4000)
4. เปิดเว็บที่:
   - http://localhost:3000 → หน้า Home
   - http://localhost:3000/info → หน้า Property Info
